# SweepstacX CLI Usage

SweepstacX helps you **scan, report, and patch** repos for dead code, unused imports, duplicates, and stale configs.  
This guide covers the basic commands with copy-paste examples.

---

## 🚀 Quick Start

```bash
# Install globally
npm i -g sweepstacx

# Verify install
sweepstacx --version
